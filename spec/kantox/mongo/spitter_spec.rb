require 'spec_helper'

describe Kantox::Mongo::Spitter do
  it 'has a version number' do
    expect(Kantox::Mongo::Spitter::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
